def verify(x: Int): Int = {
  if ((x % 3 == 0) || (x % 5 == 0)) x
  else 0
}

def auxFunction(num: Int): Int = num match {
    case 1000 => 0
    case _ => verify(num) + auxFunction(num+1)  
}

auxFunction(0)
